# Homebrew tap for Bantaba

Install the Bantaba daemon:

```sh
brew install kortiene/bantaba/bantaba
```

The formula installs `bantabad`, the local-only daemon that serves the Bantaba UI and WebSocket API.

Release artifacts are published from https://github.com/kortiene/bantaba/releases.
